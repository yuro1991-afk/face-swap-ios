# Fixtures

Use any two photos that contain a face.

This public repo does **not** ship personal photos. On the host tree, `portrait-a.jpg` / `portrait-b.jpg` are fictional generated portraits used by `prove_swap.py`.
